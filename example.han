가져오기 math

정의 더하기(x, y=10):
    반환 x + y

a, b = 1, 2
c = d = 0

반복 x, y 안에 [(1,2), (3,4)]:
    출력(x)
    출력(y)

출력(더하기(1))
출력(더하기(x=2, y=3))
출력(math.sqrt(9))

에서 math 가져오기 sqrt 별칭 루트
출력(루트(16))

정의 f():
    반환

f()